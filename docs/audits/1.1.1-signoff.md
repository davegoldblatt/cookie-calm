**Verdict: this closes my last concrete blocker. I found no defect in this delta.** I only read the four files in this snapshot and ran nothing, so none of this confirms that any test passes.

## The fix
- `interactions.js:23` now returns early for html/body before anything is added to `intentNodes`. Because the check is inside `noteChange`, it covers all three places that call it: inserted subtrees (`content.js:44`), the targets loop that caused the bug (`content.js:47`), and attributes (`content.js:56`).
- The html/body check that was already at `content.js:56` is now redundant, but it does no harm.
- `node.matches?.` is safe, because every caller passes an Element.
- The original Save-click scenario still works. `#gate` is inserted 1.8 s after the click, which is inside the 10-second window, so it is still recorded as user-opened.

## The body-class test (`registration-intent.spec.js:31–44`)
- **It would have caught the bug.** Changing the text of `#note` creates a mutation on `#note`, and `closest(CANDIDATES)` resolves it to `<body class="modal-open">`.
  - On the old code, body was marked as user-opened. `openedByUser(gate)` then returned true, the gate was protected, and `data-dismissed` never gets set, so the test fails.
  - On the new code, body is skipped.
- **Nothing else protects the gate at 11 s.** The gesture is more than 10 s old, and the gate is newly inserted. `targets` and `categories` are empty ("Change text" matches no category). The only thing the click marks as protected is body, not the gate. The page isn't in the 1.5 s grace period either.
  - So `permits` returns true and the extension clicks Close.
  - Checking `data-dismissed` proves the gate's own button did the dismissal.
- The 14-second timeout leaves about 3 s for a scan, which is enough.

## The passive control (`:22–29`)
- This is a valid positive control. Before the link arrives, the gate classifies as `''`. At 11 s it becomes a registration prompt, and with no user gesture (`gestureAt = 0`) nothing protects it.
- `toHaveCount(0)` can only pass if the Close handler removed the gate, so the test can't pass just because detection failed.
- Its timeline is not identical to the Save test, because the gate is present from page load rather than mounted at 1.8 s. It still tests the delayed-hydration path, so this doesn't block anything.

## Remaining acceptance gates
1. **Full suite:** all 79 tests pass in one run on exactly this code. Report the actual output.
2. **Live Guardian check** in the Gmail profile with 1.1.1. Don't interact with the page for the first 10 s, then confirm:
   - The gate is dismissed by its own button.
   - The article text becomes visible.
   - The popup count goes up by 1.
   - The console shows no Cookie Calm or context-invalidated errors.

   A CLI reload that disables the extension doesn't count as a failure. If the live gate isn't dismissed, don't ship 1.1.1 claiming it fixes the report.
3. Leave the Work profile and the pending 1.1.0 store review untouched.